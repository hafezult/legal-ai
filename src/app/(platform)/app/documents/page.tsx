import { WorkspacePlaceholder } from "@/components/platform/workspace-placeholder"

export default function DocumentsPage() {
  return (
    <WorkspacePlaceholder title="Documents">
      Document intelligence, privilege logs, and structured extractions will attach to
      matters here.
    </WorkspacePlaceholder>
  )
}
