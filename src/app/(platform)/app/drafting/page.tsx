import { WorkspacePlaceholder } from "@/components/platform/workspace-placeholder"

export default function DraftingPage() {
  return (
    <WorkspacePlaceholder title="Drafting">
      Clause-level drafting with lineage, playbooks, and verification gates—coming in a
      later phase without compromising this shell.
    </WorkspacePlaceholder>
  )
}
