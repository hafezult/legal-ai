import { WorkspacePlaceholder } from "@/components/platform/workspace-placeholder"

export default function MattersPage() {
  return (
    <WorkspacePlaceholder title="Matters">
      Matter workspaces, privilege boundaries, and matter-level memory will surface here.
      Create matters from the API or upcoming flows—schema is already wired on the backend.
    </WorkspacePlaceholder>
  )
}
