import { WorkspacePlaceholder } from "@/components/platform/workspace-placeholder"

export default function MemoryPage() {
  return (
    <WorkspacePlaceholder title="Memory">
      Firm and matter memory with evals, retention policies, and audit trails—foundation
      tables are staged in Prisma for when retrieval stacks land.
    </WorkspacePlaceholder>
  )
}
