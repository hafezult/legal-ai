import { WorkspacePlaceholder } from "@/components/platform/workspace-placeholder"

export default function SettingsPage() {
  return (
    <WorkspacePlaceholder title="Settings">
      Workspace preferences, integrations, and security posture will live here—Clerk
      continues to own identity while Aether owns legal context.
    </WorkspacePlaceholder>
  )
}
