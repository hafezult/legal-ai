import { WorkspacePlaceholder } from "@/components/platform/workspace-placeholder"

export default function WorkflowsPage() {
  return (
    <WorkspacePlaceholder title="Workflows">
      Orchestration graphs, human-in-the-loop approvals, and agent routing will appear
      here as the control plane expands.
    </WorkspacePlaceholder>
  )
}
